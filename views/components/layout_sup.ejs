<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5Wa5Ro9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        /* body {
            background: linear-gradient(180deg, #9BE2FE, #67D1FB);
            font-family: 'Trebuchet MS', sans-serif;
        } */
        .navbar {
            background: linear-gradient(135deg, rgba(0, 150, 255, 0.8), rgba(0, 255, 150, 0.8));
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
            border-radius: 12px;
            padding: 10px 20px;
        }
        .navbar-brand {
            text-shadow: 3px 3px 10px rgba(255, 255, 255, 0.8);
            font-weight: bold;
            font-size: 1.5rem;
        }
        .nav-link {
            color: white !important;
            font-weight: bold;
            transition: 0.3s;
        }
        .nav-link:hover {
            text-shadow: 0px 0px 10px rgba(255, 255, 255, 1);
            transform: scale(1.1);
        }
        .profile-icon {
            width: 40px;
            height: 40px;
            background: rgba(255, 255, 255, 0.3);
            box-shadow: 0 0 12px rgba(255, 255, 255, 0.6);
            border-radius: 50%;
            overflow: hidden;
        }
        .dropdown-menu {
            background: rgba(255, 255, 255, 0.9);
            border-radius: 8px;
        }
        .dropdown-item:hover {
            background: rgba(0, 150, 255, 0.5);
            color: white;
        }
        .btn-glow {
            background: linear-gradient(45deg, #00ff99, #0099ff);
            color: white;
            border: none;
            padding: 10px 20px;
            font-weight: bold;
            border-radius: 30px;
            box-shadow: 0 0 10px rgba(0, 255, 150, 0.7);
            transition: 0.3s;
        }
        .btn-glow:hover {
            box-shadow: 0 0 20px rgba(0, 255, 150, 1);
            transform: scale(1.05);
        }
    </style>
</head>

<body>
    <header>
        <%- include('navbar') %>
    </header>

    <div class="container mt-4">
        <div class="row">
            <div class="col">
